---
interface Props {
  on?: boolean;
}

const { on } = Astro.props
---
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width={1.5} stroke="currentColor" class={`swap-${on ? 'on': 'off'} fill-current w-10 h-10`}>
  <slot />
</svg>
