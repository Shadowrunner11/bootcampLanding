---
import Computer from "../Icons/Computer.astro";
import ThemeItem from "./ThemeItem.astro";
import ThemeitemList from "./ThemeitemList.astro";
import { themes } from './modules.json'

---

<div class="mockup-code w-full hidden md:block">
  <pre data-prefix="$"><code>cat temario</code></pre> 

  {themes.map(({title, topics})=>(
    <ThemeItem title={title} topics={topics}/>
  ))}
  

  <pre class="text-success"><code>Proyecto final 🚀</code></pre>
</div>

<ul class="w-full flex flex-col md:hidden">
  {
    themes.map(({title, topics})=> (
      <ThemeitemList title={title} topics={topics}/>
    ))
  }

  <h3 class='text-2xl font-bold p-4'>Proyecto final</h3>

</ul>
